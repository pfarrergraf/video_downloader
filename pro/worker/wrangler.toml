name = "downloadthat-license-server"
main = "src/index.js"
compatibility_date = "2026-07-01"

# Route this Worker at api.<your-subdomain>.geistreich.com once DNS is set up
# (see ../README.md). Until then it's reachable at the default
# workers.dev URL Cloudflare assigns on first deploy.
# [[routes]]
# pattern = "api.downloadthat.geistreich.com/*"
# zone_name = "geistreich.com"

[[d1_databases]]
binding = "DB"
database_name = "downloadthat-licenses"
database_id = "cee415b0-dad7-4ae5-a080-48872a37d057"
