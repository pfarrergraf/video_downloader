{
  "name": "downloadthat-affiliate-functions",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "find functions -name '*.js' -print0 | xargs -0 -n1 node --check && node --check affiliate-site.js"
  }
}
